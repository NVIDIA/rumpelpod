Hard-code the devcontainer repo URL

The repo is now public, so there is no need to make its URL
configurable via a REPO_URL build arg and host environment variable.
Inline the public HTTPS URL directly in the Dockerfile git clone.

A public HTTPS clone needs no authentication, so the SSH mount and the
"--ssh default" build option are dropped as well.
